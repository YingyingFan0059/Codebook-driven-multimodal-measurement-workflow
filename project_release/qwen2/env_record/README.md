# env_record

This directory is reference only.

It stores environment snapshots from earlier experiment environments, such as package versions and GPU notes. These files are useful for provenance review, but they are not the recommended installation path for new users of the open-source release.

For normal setup, use the current lightweight requirements and workflow documentation instead.

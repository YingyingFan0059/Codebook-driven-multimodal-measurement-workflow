# split_v3

This directory is example only.

It preserves a historical split layout used in earlier experiments so that file naming and downstream script assumptions remain understandable. It should not be interpreted as a full public data release, and it does not include the original video corpus.

For new work, prefer generating project-local splits through `cbma split create`.
